/**
 * Everything in JavaScript happens inside an Execution Context.
 * It Consists of two blocks: Memory & code Block.
 * 1. Memory Context: Here, it will allocate some memory to all the variables & functions & assign the value with undefined first.
 * 2. Code Context: Here, the code part will be executed line by line.
 */
